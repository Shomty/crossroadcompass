interface:
  display_name: "Crossroadcompass"
  short_description: "Repo-specific patterns and workflows for crossroadcompass"
  default_prompt: "Use the crossroadcompass repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true