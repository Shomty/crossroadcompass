diff --git a/node_modules/openastrology-library/dist/index.js b/node_modules/openastrology-library/dist/index.js
index c12e8ca..899fa5d 100644
--- a/node_modules/openastrology-library/dist/index.js
+++ b/node_modules/openastrology-library/dist/index.js
@@ -1986,9 +1986,7 @@ var _VedicAstrologyCalculator = class _VedicAstrologyCalculator {
     if (!dateRegex.test(birthInfo.dateOfBirth)) throw new Error("Invalid date format. Expected YYYY-MM-DD");
     const birthDate = new Date(birthInfo.dateOfBirth);
     if (isNaN(birthDate.getTime())) throw new Error("Invalid birth date");
-    const now = /* @__PURE__ */ new Date();
     const minDate = new Date(1800, 0, 1);
-    if (birthDate > now) throw new Error("Birth date cannot be in the future");
     if (birthDate < minDate) throw new Error("Birth date too far in the past");
     if (birthInfo.timeOfBirth) {
       const timeRegex = /^([01]?[0-9]|2[0-3]):[0-5][0-9]$/;
@@ -2687,9 +2685,7 @@ var _WesternAstrologyCalculator = class _WesternAstrologyCalculator {
     if (!dateRegex.test(birthInfo.dateOfBirth)) throw new Error("Invalid date format. Expected YYYY-MM-DD");
     const birthDate = new Date(birthInfo.dateOfBirth);
     if (isNaN(birthDate.getTime())) throw new Error("Invalid birth date");
-    const now = /* @__PURE__ */ new Date();
     const minDate = new Date(1800, 0, 1);
-    if (birthDate > now) throw new Error("Birth date cannot be in the future");
     if (birthDate < minDate) throw new Error("Birth date too far in the past");
     if (birthInfo.timeOfBirth) {
       const timeRegex = /^([01]?[0-9]|2[0-3]):[0-5][0-9]$/;
diff --git a/node_modules/openastrology-library/dist/index.mjs b/node_modules/openastrology-library/dist/index.mjs
index 8133460..0ef11e9 100644
--- a/node_modules/openastrology-library/dist/index.mjs
+++ b/node_modules/openastrology-library/dist/index.mjs
@@ -1936,9 +1936,7 @@ var _VedicAstrologyCalculator = class _VedicAstrologyCalculator {
     if (!dateRegex.test(birthInfo.dateOfBirth)) throw new Error("Invalid date format. Expected YYYY-MM-DD");
     const birthDate = new Date(birthInfo.dateOfBirth);
     if (isNaN(birthDate.getTime())) throw new Error("Invalid birth date");
-    const now = /* @__PURE__ */ new Date();
     const minDate = new Date(1800, 0, 1);
-    if (birthDate > now) throw new Error("Birth date cannot be in the future");
     if (birthDate < minDate) throw new Error("Birth date too far in the past");
     if (birthInfo.timeOfBirth) {
       const timeRegex = /^([01]?[0-9]|2[0-3]):[0-5][0-9]$/;
@@ -2637,9 +2635,7 @@ var _WesternAstrologyCalculator = class _WesternAstrologyCalculator {
     if (!dateRegex.test(birthInfo.dateOfBirth)) throw new Error("Invalid date format. Expected YYYY-MM-DD");
     const birthDate = new Date(birthInfo.dateOfBirth);
     if (isNaN(birthDate.getTime())) throw new Error("Invalid birth date");
-    const now = /* @__PURE__ */ new Date();
     const minDate = new Date(1800, 0, 1);
-    if (birthDate > now) throw new Error("Birth date cannot be in the future");
     if (birthDate < minDate) throw new Error("Birth date too far in the past");
     if (birthInfo.timeOfBirth) {
       const timeRegex = /^([01]?[0-9]|2[0-3]):[0-5][0-9]$/;
